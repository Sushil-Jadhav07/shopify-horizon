<style>
  /* ═══════════════════════════════════════════════
     ENSNE CART DRAWER — full visual override
  ═══════════════════════════════════════════════ */

  /* ── Drawer panel ── */
  .cart-drawer__dialog {
    background: #fff !important;
    color: #1a1a1a !important;
    border-left: none !important;
    box-shadow: -4px 0 32px rgba(0,0,0,0.18) !important;
    max-width: 420px !important;
    width: 100% !important;
    padding: 0 !important;
    display: flex !important;
    flex-direction: column !important;
  }

  .cart-drawer__inner {
    display: flex;
    flex-direction: column;
    height: 100%;
    overflow: hidden;
  }

  /* ── Header ── */
  .ensne-cart-drawer__header {
    display: flex !important;
    align-items: center !important;
    justify-content: space-between !important;
    padding: 20px 20px 16px !important;
    border-bottom: 1px solid #e5e5e5 !important;
    background: #fff !important;
    flex-shrink: 0 !important;
  }

  .ensne-cart-drawer__title-row {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .ensne-cart-drawer__heading {
    font-family: var(--font-label, 'Archivo Narrow', sans-serif) !important;
    font-weight: 700 !important;
    font-style: normal !important;
    font-size: 13px !important;
    letter-spacing: 0.12em !important;
    text-transform: uppercase !important;
    color: #1a1a1a !important;
    line-height: 1 !important;
    margin: 0 !important;
  }

  .ensne-cart-drawer__count {
    font-weight: 700;
    letter-spacing: 0.06em;
  }

  .ensne-cart-drawer__serving-tag {
    font-family: var(--font-label, 'Archivo Narrow', sans-serif);
    font-size: 11px;
    font-weight: 400;
    letter-spacing: 0.1em;
    color: #888;
    text-transform: lowercase;
  }

  /* Close button */
  .cart-drawer__close-button {
    color: #1a1a1a !important;
    opacity: 0.7 !important;
    flex-shrink: 0 !important;
  }

  .cart-drawer__close-button:hover {
    opacity: 1 !important;
  }

  .cart-drawer__close-button svg {
    width: 18px !important;
    height: 18px !important;
  }

  /* ── Scrollable items area ── */
  .cart-drawer__content {
    flex: 1 1 auto !important;
    overflow: hidden !important;
    display: flex !important;
    flex-direction: column !important;
  }

  .cart-drawer__items {
    flex: 1 1 auto !important;
    overflow-y: auto !important;
    padding: 0 !important;
  }

  /* ── Cart items table ── */
  .cart-drawer .cart-items__table {
    width: 100%;
    border-collapse: collapse;
  }

  .cart-drawer .cart-items__table-row {
    display: flex !important;
    align-items: flex-start !important;
    gap: 14px !important;
    padding: 16px 20px !important;
    border-bottom: 1px solid #ebebeb !important;
    position: relative !important;
  }

  /* Image cell */
  .cart-drawer .cart-items__media {
    flex-shrink: 0 !important;
    width: 76px !important;
  }

  .cart-drawer .cart-items__media-container {
    display: block !important;
    width: 76px !important;
    aspect-ratio: 1 !important;
    overflow: hidden !important;
  }

  .cart-drawer .cart-items__media-image {
    width: 100% !important;
    height: 100% !important;
    object-fit: cover !important;
    display: block !important;
    border-radius: 2px !important;
  }

  /* Details cell — name, subtitle */
  .cart-drawer .cart-items__details {
    flex: 1 1 auto !important;
    display: flex !important;
    flex-direction: column !important;
    justify-content: flex-start !important;
    padding: 0 !important;
    min-width: 0 !important;
  }

  .cart-drawer .cart-items__product-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .cart-drawer .cart-items__title {
    font-family: var(--font-label, 'Archivo Narrow', sans-serif) !important;
    font-size: 13px !important;
    font-weight: 700 !important;
    letter-spacing: 0.04em !important;
    color: #1a1a1a !important;
    text-decoration: none !important;
    text-transform: lowercase !important;
    line-height: 1.3 !important;
  }

  .ensne-cart-item__subtitle {
    font-family: var(--font-label, 'Archivo Narrow', sans-serif) !important;
    font-size: 11px !important;
    font-weight: 400 !important;
    color: #888 !important;
    letter-spacing: 0.06em !important;
    margin: 0 !important;
    text-transform: lowercase !important;
  }

  /* Variants / properties — hide or dim */
  .cart-drawer .cart-items__variants-wrapper {
    margin-top: 4px !important;
  }

  .cart-drawer .cart-items__variants {
    font-size: 11px !important;
    color: #aaa !important;
  }

  /* Quantity + remove row */
  .cart-drawer .cart-items__quantity {
    display: block !important;
    padding: 0 !important;
    width: auto !important;
  }

  .cart-drawer .cart-items__quantity-controls {
    display: flex !important;
    align-items: center !important;
    gap: 8px !important;
    margin-top: 10px !important;
  }

  /* Quantity selector pill */
  .cart-drawer .quantity-selector {
    display: flex !important;
    align-items: center !important;
    background: #1a1a1a !important;
    border-radius: 99px !important;
    border: none !important;
    padding: 0 2px !important;
    height: 28px !important;
    gap: 0 !important;
  }

  .cart-drawer .quantity-minus,
  .cart-drawer .quantity-plus {
    background: transparent !important;
    border: none !important;
    color: #fff !important;
    width: 28px !important;
    height: 28px !important;
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    font-size: 16px !important;
    font-weight: 400 !important;
    cursor: pointer !important;
    padding: 0 !important;
    line-height: 1 !important;
    flex-shrink: 0 !important;
  }

  .cart-drawer .quantity-minus svg,
  .cart-drawer .quantity-plus svg {
    width: 12px !important;
    height: 12px !important;
    stroke: #fff !important;
    fill: none !important;
  }

  .cart-drawer .quantity-selector input,
  .cart-drawer .quantity-selector [role="spinbutton"] {
    background: transparent !important;
    border: none !important;
    color: #fff !important;
    font-family: var(--font-label, 'Archivo Narrow', sans-serif) !important;
    font-size: 12px !important;
    font-weight: 400 !important;
    width: 24px !important;
    text-align: center !important;
    padding: 0 !important;
    min-width: 0 !important;
  }

  /* Remove button — text link style */
  .ensne-cart-items__remove {
    background: transparent !important;
    border: none !important;
    color: #888 !important;
    font-family: var(--font-label, 'Archivo Narrow', sans-serif) !important;
    font-size: 11px !important;
    font-weight: 400 !important;
    letter-spacing: 0.06em !important;
    text-decoration: underline !important;
    text-underline-offset: 2px !important;
    padding: 0 !important;
    cursor: pointer !important;
    line-height: 1 !important;
  }

  .ensne-cart-items__remove:hover {
    color: #1a1a1a !important;
  }

  .ensne-cart-items__remove svg {
    display: none !important;
  }

  /* Price cell */
  .cart-drawer .cart-items__price {
    flex-shrink: 0 !important;
    font-family: var(--font-label, 'Archivo Narrow', sans-serif) !important;
    font-size: 13px !important;
    font-weight: 700 !important;
    color: #1a1a1a !important;
    padding: 0 !important;
    white-space: nowrap !important;
  }

  /* Unit price */
  .cart-drawer .cart-items__unit-price-wrapper {
    display: none !important;
  }

  /* ── Summary footer — sticky, dark crimson ── */
  .cart-drawer__summary {
    flex-shrink: 0 !important;
    background: #6b0f1a !important;
    padding: 20px 20px 0 !important;
    color: #fff !important;
  }

  /* Subtotal row */
  .cart-drawer__summary .cart-totals__total {
    color: #fff !important;
  }

  .cart-drawer__summary .cart-totals__total-label {
    font-family: var(--font-label, 'Archivo Narrow', sans-serif) !important;
    font-size: 13px !important;
    font-weight: 700 !important;
    letter-spacing: 0.1em !important;
    text-transform: uppercase !important;
    color: #fff !important;
  }

  .cart-drawer__summary .cart-totals__total-value {
    font-family: var(--font-label, 'Archivo Narrow', sans-serif) !important;
    font-size: 16px !important;
    font-weight: 700 !important;
    color: #fff !important;
  }

  /* Tax note */
  .cart-drawer__summary .cart-totals__tax-note {
    color: rgba(255,255,255,0.6) !important;
    font-size: 11px !important;
  }

  .cart-drawer__summary .cart-totals__tax-note small {
    font-size: 11px !important;
  }

  /* Cart note / add order note */
  .cart-drawer__summary .cart-note__summary,
  .cart-drawer__summary .cart-note__label {
    color: rgba(255,255,255,0.75) !important;
    font-family: var(--font-label, 'Archivo Narrow', sans-serif) !important;
    font-size: 11px !important;
    font-weight: 400 !important;
    text-decoration: underline !important;
    text-underline-offset: 2px !important;
    letter-spacing: 0.06em !important;
  }

  .cart-drawer__summary .cart-actions {
    border-block: 1px solid rgba(255,255,255,0.15) !important;
  }

  /* Discount section */
  .cart-drawer__summary .cart-discount__label {
    color: rgba(255,255,255,0.75) !important;
    font-size: 11px !important;
  }

  /* Dividers */
  .cart-drawer__summary .cart-totals {
    border-block-start: none !important;
    padding-block-start: 0 !important;
    gap: 10px !important;
  }

  /* ── CTAs ── */
  .cart-drawer__summary .cart__ctas {
    padding: 16px 0 20px !important;
    display: flex !important;
    flex-direction: column !important;
    gap: 10px !important;
  }

  .cart-drawer__summary .cart__checkout-button {
    width: 100% !important;
    background: #1a1a1a !important;
    color: #fff !important;
    font-family: var(--font-label, 'Archivo Narrow', sans-serif) !important;
    font-size: 12px !important;
    font-weight: 700 !important;
    letter-spacing: 0.18em !important;
    text-transform: uppercase !important;
    border: none !important;
    border-radius: 2px !important;
    padding: 14px !important;
    cursor: pointer !important;
    transition: background 0.2s !important;
  }

  .cart-drawer__summary .cart__checkout-button:hover {
    background: #333 !important;
  }

  /* Google Pay / additional checkout buttons */
  .cart-drawer__summary .additional-checkout-buttons {
    width: 100% !important;
  }

  .cart-drawer__summary .additional-checkout-buttons > * {
    border-radius: 2px !important;
    overflow: hidden !important;
  }
</style>
